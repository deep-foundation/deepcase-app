import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deepfoundation.deepcase',
  appName: 'Deep.Case',
  webDir: 'app',
  bundledWebRuntime: false,
};

export default config;
